//Enum con los roles existentes para los usuarios
export enum ROLES {
  BASIC = 'BASIC',
  CREATOR = 'CREATOR',
  ADMIN = 'ADMIN',
}

//Enum con los niveles de acceso que tiene cada usuario en los proyectos
export enum ACCESS_LEVEL {
  DEVELOPER = 30,
  MANTEINER = 40,
  OWNER = 50,
}
